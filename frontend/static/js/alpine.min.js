(function () {
    const init = () => {
        document.querySelectorAll("[x-data]").forEach((node) => {
            node.setAttribute("data-alpine-ready", "true");
        });
    };

    document.addEventListener("DOMContentLoaded", init);

    window.Alpine = {
        start: init,
    };
})();
